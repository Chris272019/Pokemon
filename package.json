{
  "name": "pokemon-app",
  "version": "1.0.0",
  "description": "Pokemon app with React frontend and Express backend",
  "main": "server/server.js",
  "scripts": {
    "start": "cd server && node server.js",
    "build": "echo 'No build step required'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}

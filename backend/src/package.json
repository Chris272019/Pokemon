{
  "name": "poke-battle-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port 3004",
    "build": "npm install"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "18.x"
  }
} 